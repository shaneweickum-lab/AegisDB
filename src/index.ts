// Bootstrap entry point. Filled in during Phase 10 (deploy/config -> shards -> server).
// Placeholder for now so `npm run dev` has something to run during Phase 0.
console.log('AegisDB scaffold ready — implementation begins in Phase 1 (src/crypto).');
